import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // The dev badge otherwise sits on top of the hero headline during review.
  devIndicators: false,
}

export default nextConfig
